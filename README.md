# PMI Outreach CRM

Self-driving outreach for PMI Tape's four brands. Campaigns send cold emails and schedule every follow-up on their own; you handle the replies. Built to match the rest of the internal-tool suite (React CRA + Netlify Functions + Supabase + Front + Claude).

## What's in this build — Phase 1

The backbone and the cold-email engine:

- **Scheduling engine** (`netlify/functions/engine.js`, runs every 5 min) — drains a `scheduled_actions` queue, idempotent (atomic claim + `sent_log`), all timing in **America/Indiana/Indianapolis**, sends only on weekdays excluding your 8 holidays, rolls weekend/holiday sends to the next business morning, and **staggers** sends per channel so bulk imports don't trip spam filters.
- **Cold sequences** for all four campaigns — PMI to Print Shops (first email 3 weeks out, then 4/8/12, cap 4) and the three retailer campaigns (first email immediate, then 4/6/8/12/16/20/24/28/32/36/40, cap 12).
- **AI follow-ups** — Email 1 is your editable template; emails 2+ are generated by `claude-sonnet-4-6`, varied each time, governed by a per-campaign style guide + global style corrections. The next email sits queued and **previewable/editable** before it sends.
- **App UI** — dashboard, campaigns → companies → leads, lead detail (status, internal notes, samples, pause, preview/edit/reschedule/cancel upcoming emails), Manage Campaigns (templates, intervals, tone), and Settings (global style corrections, send window, stagger).
- **Manual + API lead creation** with global email uniqueness and Do-Not-Contact suppression.

## Coming in later phases (not in this build)

- **Phase 2 — Lead sources:** Apollo chat import, spreadsheet import, Zoho Form intake, saved searches + "Suggested leads to add."
- **Phase 3 — Reply-aware AI:** Front inbound webhooks, Cold→Dialogue (auto-reply excluded), bounce→Inactive, tag→status (bidirectional), free-form comment commands, Dialogue drafts + Current-Customer comments.
- **Phase 4 — Calendar** with per-day push-forward.

## Setup

1. **Database** — open the Supabase SQL editor for project `zhvfcipveeeybczzmues` and run `supabase/migration.sql`. It creates every table and seeds the four campaigns + Email 1 templates.
2. **Front** — make sure each sender address (`andrew@pmitape.com`, `andrew@floorbondtape.com`, `andrew@tapegenie.com`, `Andrew@deckbond.com`) exists as a **channel**. Create an API token with scopes `messages:send`, `drafts:write`, and comment access. Note your teammate id (`tea_…`) for `FRONT_AUTHOR_ID`.
3. **Env vars** (Netlify → Site settings → Environment, and a local `.env` for dev) — copy `.env.example` and fill in `SUPABASE_SERVICE_ROLE_KEY`, `FRONT_API_TOKEN`, `FRONT_AUTHOR_ID`, `ANTHROPIC_API_KEY`, `APP_ACCESS_TOKEN`, and the matching `REACT_APP_ACCESS_TOKEN`.
4. **Deploy** — push to a GitHub repo under `andmjames`, connect it in Netlify. Build settings come from `netlify.toml` (build `CI= npm run build`, publish `build`, functions `netlify/functions`). The engine schedule is already declared there.

## Look & feel

The UI is matched to the Equipment Troubleshooting app: same monochrome design tokens (white / near-black, hairline `.5px` borders, off-white `#f7f7f5` surfaces), the same white header with the PMI logo + divider + page title, and the same button/field/badge styling. The real logo ships in `src/logo.js`. If that app's tokens ever change, they live in the `:root` block at the top of `src/index.css`.

## Notes / things to verify on first live run

- The Front client (`netlify/functions/_lib/front.js`) is written to Front's documented API. Confirm the channel send + draft-reply payloads against your workspace on the first send; Front occasionally wants `to[0]` form-encoding rather than a JSON array.
- The engine kicks itself on lead creation so immediate first emails go promptly; otherwise it catches them within ~5 minutes.
- Editing an upcoming email on a lead marks it as a per-lead override and won't be overwritten by global regeneration.
- Outbound emails ask Front to append your **default signature** (`should_add_default_signature`). For this to attach a *personal* signature, the API token may need the "Private Resources" scope (shared/channel signatures work without it). To pin one specific signature, set `FRONT_SIGNATURE_ID`.
